log
/storage
/tmp
node_modules

# Vite Ruby
/public/vite*
# Vite uses dotenv and suggests to ignore local-only env files. See
# https://vitejs.dev/guide/env-and-mode.html#env-files
*.local

/.playwright
/.playwright-cli
/.ruby-lsp

# Secrets — the master key never gets committed (it unlocks credentials.yml.enc, which is committed).
/config/master.key
.claude/settings.local.json

# Ignore key files for decrypting credentials and more.
/config/*.key

