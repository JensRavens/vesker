log
/storage
/tmp
node_modules

# Vite Ruby
/public/vite*
# Vite uses dotenv and suggests to ignore local-only env files. See
# https://vitejs.dev/guide/env-and-mode.html#env-files
*.local

/.playwright
/.playwright-cli
/.ruby-lsp

# Secrets — never commit. Production reads SECRET_KEY_BASE from the environment.
/config/master.key
/config/credentials.yml.enc
.claude/settings.local.json
